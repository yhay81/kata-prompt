# Metrics

匿名のUUIDをSHA-256で一方向変換し、イベント名と日付だけをD1へ保存します。入力文、完成文、検索語、選択した型のID、IPアドレスは保存しません。同じ匿名利用者・イベント・日付は一件にまとめ、35日後に削除します。

| Event      | Meaning                        |
| ---------- | ------------------------------ |
| `visited`  | 作業台または関連ページを開いた |
| `selected` | 別の型を選んだ                 |
| `filled`   | 型の入力を変更した             |
| `copied`   | 完成文をコピーした             |
| `saved`    | 完成文を端末へ保存した         |
| `exported` | MarkdownまたはJSONを書き出した |
| `returned` | 別の日に再訪した               |

中核成果は`copied`です。`npm run metrics`は、全期間と直近7日の人数、選択・入力・コピー・保存・書き出し・再訪の人数と転換率を集計JSONで返します。本文や個別識別子は出力しません。
